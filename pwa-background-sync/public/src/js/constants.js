/**
 * https://github.com/DanielRaj1610/msc-thesis-pwa-api clone the repo and run ``npm i`` ,
 * then run ``npm start`` server should start at http://localhost:4000
 * BACKEND_URL is currently hardcoded to a deployed url, which might not work at the time of usage
 */
var constants = {
  BACKEND_URL: "https://msc-thesis-pwa-api-production.up.railway.app", // or use http://localhost:4000 after starting the backend server
};
